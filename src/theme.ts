import { DefaultTheme } from "styled-components";

export const theme: DefaultTheme = {
  bgColor: "#87ceeb", // Sky blue background
  textColor: "#333", // Darker text color
  boardColor: "#f5f5f5", // Board color
  cardColor: "#fff", // Card color
};
